# Production Build Setup for Vercel Serverless

We use Vercel Serverless to deploy the Express backend.
The root package.json is prepared to deploy the `backend` directly if you push this folder to Vercel.

If deploying via CLI:
```bash
# In /Golf/backend
npx vercel
```
